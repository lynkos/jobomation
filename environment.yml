name: job_env
channels:
  - conda-forge
dependencies:
  - python=3.14
  - pip
  - sqlite
  - pip:
      - sqlalchemy
      - pydantic
      - pydantic-settings
      - httpx
      - beautifulsoup4
      - lxml
      - python-dotenv
      - pytest